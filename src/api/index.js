
'use strict';

const compareImages = require( './compare-images' );
const indexSite = require( './index-site' );
const takeSnapShots = require( './take-snapshots' );

module.exports = { compareImages:    compareImages,
                   indexSite    :    indexSite,
                   takeSnapShots:   takeSnapShots
                 };